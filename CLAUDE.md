## gstack
Use /browse from gstack for all web browsing. Never use mcp__claude-in-chrome__* tools.
The only gstack skills I use are: /investigate, /codex, /office-hours, /qa.
Do not invoke, suggest, or run any other gstack skill unless I explicitly name it.
